# The Book of ASCII Communication

Source repo for the public GitHub Pages edition of the ASCII communication book.

The current published material includes Chapter 1, which showcases one source artifact, `Saint Basil's Cathedral`, in three embedded forms:

- `text`
- `image`
- `image+text`

The site is deployed from `frontend/` via GitHub Pages Actions, with room for additional chapters as the research book grows.
