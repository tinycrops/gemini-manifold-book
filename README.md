# Gemini Manifold Chapter 1

Public GitHub Pages snapshot for the first chapter in the ASCII communication book.

This chapter showcases one source artifact, `Saint Basil's Cathedral`, in three embedded forms:

- `text`
- `image`
- `image+text`

The site is deployed from `frontend/` via GitHub Pages Actions.
